<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="bg-white shadow-lg rounded-2xl p-8 max-w-xl w-full text-center">
      <h1 class="text-3xl font-bold text-green-600 mb-4">Payment Successful!</h1>
      <p class="text-gray-700 mb-2">Thank you for your order. Your payment has been successfully processed.</p>
      
      <div class="mt-6 text-left">
        <p class="text-sm text-gray-600"><strong>Order ID:</strong> <%= orderDetails._id %></p>
        <p class="text-sm text-gray-600 mb-4"><strong>Total Amount:</strong>$<%=finalAmount %></p>
  
        <h2 class="text-lg font-semibold mb-2 text-gray-800">Order Items:</h2>
        <ul class="space-y-4">
          <% orderDetails.orderedItem.forEach(item => { %>
            <li class="bg-gray-50 p-4 rounded-lg shadow-sm border">
              <p><strong>Product:</strong> <%= item.productId.productName %></p>
              <p><strong>Quantity:</strong> <%= item.quantity %></p>
              <p><strong>Price:</strong>$<%= item.productId.salePrice %></p>
            </li>
          <% }) %>
          
        </ul>
      </div>
      <a href="/orders" class="inline-block mt-6 bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded transition">
        View Your Orders
      </a>
    </div>
  </body>
  
</html>